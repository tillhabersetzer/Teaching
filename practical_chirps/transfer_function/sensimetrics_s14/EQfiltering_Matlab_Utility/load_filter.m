function [h,Fs] = load_filter(filename)
%	[h,Fs] = load_filter(filename)
%
%	Function to load filters used by EQ_filtering.
%
%	filename = name of filter file.  MUST include '.bin' extension.
%
%	h = imulse response of filter
%	Fs = sampling rate of filter
%
